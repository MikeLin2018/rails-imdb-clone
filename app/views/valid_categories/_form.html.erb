<%= form_with(model: valid_category, local: true) do |form| %>
  <% if valid_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(valid_category.errors.count, "error") %> prohibited this valid_category from being saved:</h2>

      <ul>
        <% valid_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
